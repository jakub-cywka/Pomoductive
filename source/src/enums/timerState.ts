/**
 * @copyright OpenSourced
 * @author Jakub Cywka
 * @description An enum representing the possible time states to use in app.
 * @version 0.1.0
*/
export enum TimerState {
    Running = 'running',
    Paused = 'paused',
    Reseted = 'reseted'
};