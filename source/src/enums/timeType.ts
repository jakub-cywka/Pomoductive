/**
 * @copyright OpenSourced
 * @author Jakub Cywka
 * @description An enum representing the possible time types to use by pomodoro lifecycle.
 * @version 0.1.0
*/
export enum TimeType {
    Work = 'work',
    Break = 'break'
};